# VisualAudit

Set `OPENAI_API_KEY` in `.env.local` before running the app:

```bash
OPENAI_API_KEY=your_key_here
```

Then start the Next.js app with:

```bash
npm run dev
```

The vision observation MVP is available at `/visual-audit`.
